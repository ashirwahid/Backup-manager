# Build and deploy backend/ to Azure App Service (Linux Python 3.11).
# Oryx installs requirements-prod.txt on the server into antenv.

name: Build and deploy Python app to Azure Web App - backup-manager

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Validate backend deploy layout
        working-directory: backend
        run: |
          test -f server.py
          test -f requirements-prod.txt
          test -f startup.sh
          chmod +x startup.sh

      # Upload only backend/ so server.py is at /home/site/wwwroot/server.py
      - name: Upload artifact for deployment jobs
        uses: actions/upload-artifact@v4
        with:
          name: python-app
          path: backend

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: python-app

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_9904277AF8354557A071EA007094353F }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_BCB62555DA2B467EAB259C93DCCB61B5 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_7EE9661EF97B475F895D3364EBE17BBC }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: backup-manager
          slot-name: Production
